var d = document, w = window;

var Balance = function () {
    this.config = {
        1: {
            'request': 'https://support.selectel.ru/?action_method=GET'
        }
    }

    var config = this.config;
}